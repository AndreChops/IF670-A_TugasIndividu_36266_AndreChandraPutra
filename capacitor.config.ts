import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TugasIndividu01_36266_AndreChandra',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
