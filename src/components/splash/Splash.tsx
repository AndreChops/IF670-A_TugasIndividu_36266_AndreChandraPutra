import { IonPage, IonText } from '@ionic/react';
import './Splash.css';

interface SplashProps { }

const Splash: React.FC<SplashProps> = () => {
    return(
    <div className="splashContainer"></div>
  );
};

export default Splash;
